<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myproduct.lib.common_ui">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- 配置 Glide -->
        <meta-data
            android:name="com.example.myproduct.lib.common_ui.utils.glide.config.GlideConfiguration"
            android:value="GlideModule" />

    </application>

</manifest>
